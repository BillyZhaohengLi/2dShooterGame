#pragma once
#include "const.h"
#include "button.h"
//helper class only used to add buttons into a level; due to the sheer amount of buttons it was separated from main and placed here.
void add_buttons(AllButtons& buttons_in_level);